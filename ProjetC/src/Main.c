#include <stdio.h>
#include <IHM.h>
#include <Util.h>
#include <stdlib.h>
#include <string.h>
int main (void)
{
	IHM();

	/*int err=0;
	while (1)
	{
		char*buff=NULL;
		buff=malloc(50);
		memset(buff,0,50);
		char*buff2=NULL;
		buff2=malloc(50);
		memset(buff,0,50);
		gets(buff);
		gets(buff2);
		err=TestType(buff,buff2);
		printf("Err : <%d>",err);
	}
	*/
	return 0;
}
